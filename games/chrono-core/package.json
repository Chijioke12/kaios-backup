{
  "name": "chrono-core",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "package-omnisd": "npm run build && cd dist && zip -r ../application.zip . && cd .. && echo '{\"version\": 1, \"manifestURL\": \"app://chrono-core/manifest.webapp\"}' > metadata.json && touch update.webapp && zip chrono-core-omnisd.zip application.zip metadata.json update.webapp && rm application.zip metadata.json update.webapp"
  },
  "dependencies": {
    "@vitejs/plugin-legacy": "^5.4.3",
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "terser": "^5.47.1"
  },
  "devDependencies": {
    "@types/react": "^18.2.66",
    "@types/react-dom": "^18.2.22",
    "@vitejs/plugin-react": "^4.2.1",
    "typescript": "^5.2.2",
    "vite": "^5.2.0"
  }
}
